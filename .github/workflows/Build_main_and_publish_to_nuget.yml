# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Build_main_and_publish_to_nuget --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Build_main_and_publish_to_nuget

on:
  push:
    branches:
      - main

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: PublishToGitHubNuget, PublishToNuget, PublishBinary'
        run: ./build.cmd PublishToGitHubNuget PublishToNuget PublishBinary
        env:
          NuGetApiKey: ${{ secrets.NUGET_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: nuget'
        uses: actions/upload-artifact@v3
        with:
          name: nuget
          path: artifacts/nuget
      - name: 'Publish: app'
        uses: actions/upload-artifact@v3
        with:
          name: app
          path: artifacts/app
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: PublishToGitHubNuget, PublishToNuget, PublishBinary'
        run: ./build.cmd PublishToGitHubNuget PublishToNuget PublishBinary
        env:
          NuGetApiKey: ${{ secrets.NUGET_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: nuget'
        uses: actions/upload-artifact@v3
        with:
          name: nuget
          path: artifacts/nuget
      - name: 'Publish: app'
        uses: actions/upload-artifact@v3
        with:
          name: app
          path: artifacts/app
